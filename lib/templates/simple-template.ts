import type { TemplateConfig } from "@/types/template";

export const simpleTemplate: TemplateConfig = {
  id: "simple",
  name: "Simple",
  description: "Plain text layout using your existing About content without extra sections.",
  version: "1.0.0",
  sections: [],
};
